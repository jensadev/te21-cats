{% extends "layout.njk" %}

{% block content %}
<main class="wrapper">
  <h1>{{ title }}</h1>
  <p>Jag kan nu byta namn på min route till /cats för att visa en lista på alla katter.</p>
  <p>Nästa steg blir sedan att göra sidor för varje enskild katt.</p>
  <p>Jag vill <a href="/cats/new">registrera min katt.</a></p>
  <p>Jag vill <a href="/newbreed">lägga till en ny kattras.</a></p>
  <ul class="catlist">
    {% for cat in cats %}
      <li><a href="/cats/{{cat.id}}">{{ cat.name }}</a>{{ cat.breed }}<a href="/cats/{{ cat.id }}/delete">Ta bort katt</a></li>
    {% endfor %}
  </ul>
</main>
{% endblock %}