{% extends "layout.njk" %}

{% block content %}
<main class="wrapper">
  <h1>{{ title }}</h1>
  <ul>
    {% for cat in cats %}
      <li>{{ cat.name }}, {{ cat.age }}, {{ cat.breed }}</li>
    {% endfor %}
</main>
{% endblock %}